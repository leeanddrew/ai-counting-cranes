# Dockerfile.frontend
FROM node:18

WORKDIR /app

COPY . .

RUN npm install

# 🛑 Disable ESLint at build time (even if Next.js tries to run it)
ENV NEXT_DISABLE_ESLINT=true

RUN npm run build

EXPOSE 3000
CMD ["npm", "start"]